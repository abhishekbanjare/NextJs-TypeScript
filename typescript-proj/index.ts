//@ts-ignore
function addTwoNumbers(a: number, b: number): number {
    return a + b;
}

// Example usage:
const num1: number = 5;
const num2: number = 30;

const result:number = addTwoNumbers(num1, num2);
console.log(`The sum of ${num1} and ${num2} is ${result}`);
